export interface Modal {
    name: string;
    street_address: string;
    postal_code: string;
    locality: string;
    weekday_id: number;
    opens: number;
    closes: number;
    open24h: boolean;
}